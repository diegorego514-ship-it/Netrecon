# ipaddress
import ipaddress as ipaddress 
import nmap as nmap

def list_target_ipaddress():

    if str('then print the ipaddress of the target: '):
        print(ipaddress,  + 'str', 'as', list_target_ipaddress)

def find_target_ip_address():

        def then():
            if find_target_ip_address():
                then()
                print('You have been hacked!\n:')
        
            def failed_to_get_target_ipaddress():
                if failed_to_get_target_ipaddress():
                    then()
                    print('ConnectionAbortedError')

        
            def target_adquired(open_port = 22):
                (open_port == 22 + target_adquired, list_target_ipaddress and find_target_ip_address)
                then()
                print('target_locked_on')
